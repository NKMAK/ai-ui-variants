# UI Variant Preview MVP 引き継ぎ

作成: 2026-05-28 00:29 JST

## 現在地

コンテキストが大きくなったため、ユーザー指示に従いここで停止する。

- ブランチ: `develop`
- 作業ツリー: clean
- 最新コミット: `7df8d1d variant操作のロジック層を用意`
- Phase 1: 完了
- Phase 2: 完了
- Phase 3: 完了
- Phase 4-1: 完了
- 次に着手するもの: Phase 4-2 `plan/ui-variant-preview-mvp/04-panel/02-components.md`

## コミット済み

- `466a1fe` MVP の足場とデモアプリを用意
- `f528cb8` server のパス変換と mock 生成器を用意
- `f082537` snapshot と worktree による patch 生成を用意
- `9a3bd4b` Local Agent Server を Vite plugin に結線
- `8f563fb` overlay 注入と Shadow DOM 基盤を用意
- `4946242` Inspector で source 選択と session 開始を結線
- `7df8d1d` variant 操作のロジック層を用意

## 検証済み

- `pnpm install`
- `pnpm --filter vite-plugin-ui-variants exec tsc --noEmit`
- `pnpm --filter demo-app exec tsc --noEmit`
- Phase 2 server API:
  - `POST /__ui_agent/session/start` 成功
  - 二重 start が 409
  - `generate-variants` で 3 variants ready
  - `preview` で `SaveButton.tsx` が書き換わる
  - `discard` で base snapshot に復元
  - dirty 対象ファイルの start が 409
  - `apply` で選択 patch が残ることを確認後、検証用変更は元に戻した
  - `git worktree list` に残骸なし
- `validatePatch`:
  - safe patch は OK
  - `package.json` denylist は NG
  - 4 files 超過は NG
  - 101 changed lines は NG
- Phase 3 overlay:
  - overlay script 注入あり
  - Shadow DOM 配下に overlay 描画
  - toggle が `Inspector Off` → `Inspector On`
  - hover で HighlightBox 表示
  - click で `src/components/SaveButton.tsx:3:5` 表示
  - session id 表示
  - dirty 対象ファイル click で 409 エラー表示

## 注意点

- Phase 3-1 で、overlay 側 TSX が host React JSX 設定に引っ張られて toggle が反応しない問題があった。`client/index.tsx` / `App.tsx` / `InspectorToggle/index.tsx` に `/** @jsxImportSource preact */` を明示して修正済み。
- `data-ui-source` の実値は `src/components/SaveButton.tsx:3:5`。計画 docs の一部に `:2:5` と書かれている箇所があるが、実ファイルに合わせた `:3:5` が正。
- Phase 4-1 の `useVariants` は UI 未結線。実地の HMR 切替検証は Phase 4-2 後に実施する。
- 一時検証で `.ui-agent/` と `.playwright-mcp/` が生成されたが、gitignore 対象で削除済み。

## 次の作業

1. `AGENTS.md` / `CLAUDE.md` / `plan/ui-variant-preview-mvp/summary.md` を読む。
2. `plan/ui-variant-preview-mvp/04-panel/summary.md` と `04-panel/02-components.md` を読む。
3. `subagent-exec` 方針に従い、Phase 4-2 を worker に委任する。
4. worker 戻り後、メインで以下を検証する。
   - input に入力してから ON → SaveButton click → 指示入力 → 生成
   - Variant A/B/C 表示
   - 前へ/次へでファイルが切り替わり、HMR で画面が変わる
   - input の値が保持される
   - busy 中の連打で壊れない
   - apply で選択変更だけが残る
   - discard で base に戻る
   - `git worktree list` に残骸なし
   - `pnpm --filter vite-plugin-ui-variants exec tsc --noEmit`
   - `pnpm --filter demo-app exec tsc --noEmit`
5. Phase 4 完了後、`plan/ui-variant-preview-mvp/summary.md` と `04-panel/summary.md` の状態を更新してコミットする。

## 未着手

- Phase 4-2: Panel UI 群 + App 組み込み
- Phase 5: Claude Code headless generator
